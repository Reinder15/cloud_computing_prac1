
subnet: 10.24.38.x/24
VLAN: 2438
Default Gateway: 10.24.38.1/24

35448901 (354489-vm1):
hostname: node1.klein-mkb-host.nl
IP adres: 10.24.38.2
root - Strike1023

Tailscale entry: https://10.24.38.2:8006/#v1:0:18:4:::::::
root - Strike1023


35448902 (354489-vm2):
hostname: node2.klein-mkb-host.nl
IP adres: 10.24.38.3
root - Strike1023

35448903 (354489-vm3):
hostname: node3.klein-mkb-host.nl
IP adres: 10.24.38.4
root - Strike1023


Voor setup:
1. ls /etc/apt/sources.list.d/
Output zou moeten zijn:
ceph.sources  debian.sources  pve-enterprise.sources
2. rm /etc/apt/sources.list.d/pve-enterprise.sources
3. rm /etc/apt/sources.list.d/ceph.sources
3. apt update
4. nano /etc/hosts
Voeg toe:
127.0.0.1 localhost.localdomain localhost
10.24.38.2 node1.klein-mkb-host.nl node1
10.24.38.3 node2.klein-mkb-host.nl node2
10.24.38.4 node3.klein-mkb-host.nl node3


Cluster gemaakt.
https://pve.proxmox.com/wiki/Cluster_Manager
op node 1: 
    pvecm create cluster1
Vervolgens voor node2 en node3:
    pvecm add 10.24.38.2

Installeer ceph:
https://pve.proxmox.com/wiki/Deploy_Hyper-Converged_Ceph_Cluster
1. pveceph install --repository no-subscription
2. pveceph init --network 10.24.38.0/24

STAP 1: FIX NVME-CLI PROBLEEM
Op alle 3 nodes uitvoeren:
# Voeg Debian repositories toe 
cat /etc/apt/sources.list.d/debian.sources
# Zo niet, dan:
# echo "deb http://deb.debian.org/debian trixie main contrib non-free non-free-firmware" > /etc/apt/sources.list

# Update en installeer nvme-cli
apt update
apt install nvme-cli

# Installeer Ceph
pveceph install --repository no-subscription

STAP 2: INITIALISEER CEPH (alleen op node1)
pveceph init --network 10.24.38.0/24

STAP 3: MAAK CEPH MONITORS AAN
Op alle 3 nodes:
pveceph mon create

STAP 4: MAAK CEPH MANAGERS AAN
Op alle 3 nodes:
pveceph mgr create

STAP 5: CREËER OSD'S (op alle nodes)
# Check beschikbare disks
lsblk
pvesm status

# Maak partitie met parted zoals hierboven beschreven
parted /dev/sdX  # of de juiste disk
(parted) print free
(parted) mkpart OSD1 <start> <end>
(parted) quit

# Creëer OSD
pveceph osd create /dev/sda4  # gebruik de nieuwe partitie

STAP 6: MAAK CEPH POOL AAN (op node1)
# Voor VM's en containers
pveceph pool create ceph-pool --add_storages

STAP 6B: SETUP CEPHFS (Shared filesystem across nodes)
# Maak Metadata Servers (MDS) aan op alle 3 nodes
# Op node1:
pveceph mds create
# Op node2:
pveceph mds create
# Op node3:
pveceph mds create

# Maak CephFS pools en filesystem (op node1)
ceph osd pool create cephfs_data 32
ceph osd pool create cephfs_metadata 16
ceph fs new cephfs cephfs_metadata cephfs_data

# Voeg toe aan Proxmox storage
pvesm add cephfs cephfs --path /mnt/pve/cephfs --content backup,vztmpl,iso,snippets --nodes node1,node2,node3

# Verify
ceph mds stat
ceph fs ls
ls -la /mnt/pve/cephfs

# Clone git repo naar shared storage
cd /mnt/pve/cephfs
git clone https://github.com/Reinder15/cloud_computing_prac1.git
# Nu toegankelijk op alle nodes via: /mnt/pve/cephfs/cloud_computing_prac1

STAP 6C: SSH KEY MANAGEMENT (Voor multi-node VM deployment)
# Maak SSH keys aan in CephFS shared storage zodat je vanaf elke node kunt deployen
# Dit is belangrijk voor VM automation scripts die SSH keys nodig hebben

# Optie 1: Nieuwe SSH key genereren in shared storage
mkdir -p /mnt/pve/cephfs/.ssh
chmod 700 /mnt/pve/cephfs/.ssh
ssh-keygen -t rsa -b 4096 -f /mnt/pve/cephfs/.ssh/id_rsa -N ""
chmod 600 /mnt/pve/cephfs/.ssh/id_rsa
chmod 644 /mnt/pve/cephfs/.ssh/id_rsa.pub

# Optie 2: Kopieer bestaande key naar shared storage
# cp ~/.ssh/id_rsa /mnt/pve/cephfs/.ssh/id_rsa
# cp ~/.ssh/id_rsa.pub /mnt/pve/cephfs/.ssh/id_rsa.pub
# chmod 600 /mnt/pve/cephfs/.ssh/id_rsa
# chmod 644 /mnt/pve/cephfs/.ssh/id_rsa.pub

# Verify: Key moet nu toegankelijk zijn op alle nodes
ls -la /mnt/pve/cephfs/.ssh/
# Output: -rw------- 1 root root 3243 ... id_rsa
#         -rw-r--r-- 1 root root  743 ... id_rsa.pub

# Deze key wordt gebruikt door deployment scripts voor SSH authenticatie naar VMs
# Scripts gebruiken: ssh -i /mnt/pve/cephfs/.ssh/id_rsa user@host

STAP 7: WORDPRESS IN LXC (KLANT 1 - TRAINING)
https://pve.proxmox.com/pve-docs/pve-admin-guide.html#chapter_pct
# Download Ubuntu/Debian LXC template
pveam update
pveam available | grep ubuntu
pveam download local ubuntu-25.04-standard_25.04-1.1_amd64.tar.zst
pveam list local

# Maak LXC container
pct create 100 local:vztmpl/ubuntu-22.04-standard_22.04-1_amd64.tar.zst \
  --hostname wordpress-training \
  --memory 2048 \
  --net0 name=eth0,bridge=vmbr0,ip=10.24.38.10/24,gw=10.24.38.1 \
  --nameserver 8.8.8.8 \
  --onboot 1 \
  --start 1 \
  --storage ceph-pool

# Start container en installeer WordPress MANUEEL:
pct start 100
pct enter 100

# In de container:
https://developer.wordpress.org/advanced-administration/before-install/howto-install/
apt update && apt upgrade -y
apt install apache2 mysql-server php php-mysql libapache2-mod-php php-cli php-curl php-gd php-mbstring php-xml php-xmlrpc -y
wget https://wordpress.org/latest.tar.gz
tar -xzf latest.tar.gz
mv wordpress /var/www/html/

https://developer.wordpress.org/advanced-administration/before-install/creating-database/
mysql
mysql> CREATE DATABASE myWordPressDatabase;  
mysql> CREATE USER "admin"@"localhost" IDENTIFIED BY "admin";
mysql> GRANT ALL PRIVILEGES ON myWordPressDatabase.* TO "admin"@"localhost";
mysql> FLUSH PRIVILEGES;  
mysql> EXIT

touch /var/www/html/wp-config.php
# Vul wp-config.php in met database gegevens


STAP 8: WORDPRESS IN VM (KLANT 2 - HA)
# BELANGRIJK: Installeer cloud-init VOOR het maken van template!
# Dit zorgt ervoor dat clones automatisch de juiste netwerk configuratie krijgen.

# Download Ubuntu/Debian ISO
# Maak VM met minimaal 2GB RAM
# Installeer OS manueel
# Installeer wordpress manueel zoals in LXC, maar dan op VM
# Installeer cloud-init (zie verderop)

Op node1:
cd /var/lib/vz/template/iso
wget https://releases.ubuntu.com/22.04/ubuntu-22.04.5-live-server-amd64.iso

# Optie A: MANUELE INSTALLATIE (simpel en betrouwbaar)
# https://pve.proxmox.com/pve-docs/qm.1.html
qm create 500 \
  --name wordpress-crm1 \
  --memory 4096 \
  --cores 2 \
  --net0 virtio,bridge=vmbr0 \
  --scsihw virtio-scsi-pci \
  --scsi0 ceph-pool:30 \
  --ide2 local:iso/ubuntu-22.04.5-live-server-amd64.iso,media=cdrom \
  --boot "order=scsi0;ide2" \
  --ostype l26 \
  --onboot 1

qm start 500

# Nu installeren via Proxmox GUI Console:
# 1. Proxmox GUI -> VM 200 -> Console
# 2. Volg Ubuntu installer:
#    - Language: English
#    - Keyboard: English (US)
#    - Install Ubuntu Server
#    - Network: Manual
#      - Subnet: 10.24.38.0/24
#      - Address: 10.24.38.30
#      - Gateway: 10.24.38.1
#      - Name servers: 8.8.8.8
#    - Proxy: (leeg)
#    - Mirror: (default)
#    - Storage: Use entire disk
#    - Profile:
#      - Name: client
#      - Server: wordpress-crm-prod
#      - Username: client
#      - Password: SecurePass123!   
#    - SSH: Install OpenSSH server (✓)
#    - Snaps: (skip)
#    - Reboot
# 3. Na reboot: ssh client@10.24.38.30

# Installeer WordPress in VM (na SSH login):
sudo apt update && sudo apt upgrade -y
sudo apt install -y apache2 mysql-server php libapache2-mod-php \
    php-mysql php-curl php-gd php-mbstring php-xml php-xmlrpc php-zip curl wget

sudo mysql
# CREATE DATABASE wordpress_db;
# CREATE USER 'wpuser'@'localhost' IDENTIFIED BY 'wpuser';
# GRANT ALL PRIVILEGES ON wordpress_db.* TO 'wpuser'@'localhost';
# FLUSH PRIVILEGES;
# EXIT;

cd /tmp
wget https://wordpress.org/latest.tar.gz
tar -xzf latest.tar.gz
sudo rm -f /var/www/html/index.html
sudo cp -r wordpress/* /var/www/html/
sudo chown -R www-data:www-data /var/www/html/
sudo chmod -R 755 /var/www/html/

cd /var/www/html
sudo cp wp-config-sample.php wp-config.php
sudo nano wp-config.php
# Pas aan: DB_NAME=wordpress_db, DB_USER=wpuser, DB_PASSWORD=SecurePass123!

sudo systemctl restart apache2

# Test: http://10.24.38.60

# STAP 8.1: INSTALLEER CLOUD-INIT (BELANGRIJK VOOR TEMPLATE!)
# Dit moet gebeuren VOOR het maken van de template
sudo apt update
sudo apt install -y cloud-init

# Configureer cloud-init datasources voor Proxmox
sudo cat > /etc/cloud/cloud.cfg.d/99-proxmox.cfg << 'EOF'
datasource_list: [ NoCloud, ConfigDrive ]
ssh_pwauth: true
chpasswd:
  expire: false
disable_root: false
EOF

sudo cat > /etc/ssh/sshd_config << 'EOF'
# Standaard SSH configuratie
PermitRootLogin yes
PasswordAuthentication yes
PubkeyAuthentication yes
ChallengeResponseAuthentication no
UsePAM yes
X11Forwarding yes
PrintMotd no
AcceptEnv LANG LC_*
Subsystem sftp /usr/lib/openssh/sftp-server
EOF

sudo systemctl restart sshd

# Configureer passwordless sudo voor deployment script
echo "client ALL=(ALL) NOPASSWD: ALL" | sudo tee /etc/sudoers.d/client
sudo chmod 0440 /etc/sudoers.d/client

# Enable cloud-init services
sudo systemctl enable cloud-init cloud-init-local cloud-config cloud-final

# Clean cloud-init state (klaar voor template)
sudo cloud-init clean --logs --seed

# Shutdown VM (klaar voor template conversie)
sudo shutdown -h now

# STAP 8.2: VOEG CLOUD-INIT DRIVE TOE EN MAAK TEMPLATE
# Na shutdown, voer uit op Proxmox host:

# BELANGRIJK: Gebruik shared SSH key in CephFS voor toegang vanaf alle nodes
# Genereer SSH key in shared storage als je die nog niet hebt:
mkdir -p /mnt/pve/cephfs/.ssh
chmod 700 /mnt/pve/cephfs/.ssh
ssh-keygen -t rsa -b 4096 -f /mnt/pve/cephfs/.ssh/id_rsa -N ""
chmod 600 /mnt/pve/cephfs/.ssh/id_rsa
chmod 644 /mnt/pve/cephfs/.ssh/id_rsa.pub

# Of kopieer bestaande key naar shared storage:
# cp ~/.ssh/id_rsa /mnt/pve/cephfs/.ssh/id_rsa
# cp ~/.ssh/id_rsa.pub /mnt/pve/cephfs/.ssh/id_rsa.pub
# chmod 600 /mnt/pve/cephfs/.ssh/id_rsa

# Voeg cloud-init drive toe
qm set 500 --ide2 ceph-pool:cloudinit
qm set 500 --nameserver 8.8.8.8
qm set 500 --ciuser client

# Configureer SSH key authenticatie (AANBEVOLEN):
qm set 500 --sshkeys /mnt/pve/cephfs/.ssh/id_rsa.pub

# OF gebruik password (minder veilig):
# qm set 500 --cipassword SecurePass123!

# BELANGRIJK: Stel GEEN ipconfig0 in op de template!
# Dit wordt automatisch geconfigureerd per clone door het deployment script.
# Elke clone krijgt zijn eigen IP via: qm set ${VMID} --ipconfig0 ip=X.X.X.X/24,gw=...

# Maak er nu een template van:
qm template 500

# Test: Deploy nieuwe VMs met automatische netwerk configuratie!
bash /mnt/pve/cephfs/cloud_computing_prac1/install-wordpress-vm.sh 301 10.24.38.31 wordpress-client1

# Optie B: CLOUD-INIT IMAGE (vooraf geconfigureerd alternatief)
# Als je niet wilt manueel installeren, gebruik Ubuntu cloud image:
# wget https://cloud-images.ubuntu.com/jammy/current/jammy-server-cloudimg-amd64.img
# qm importdisk 500 jammy-server-cloudimg-amd64.img ceph-pool
# qm set 500 --scsi0 ceph-pool:vm-500-disk-0
# qm set 500 --ide2 ceph-pool:cloudinit
# qm set 500 --boot c --bootdisk scsi0
# qm set 500 --ipconfig0 ip=10.24.38.30/24,gw=10.24.38.1
# qm set 500 --nameserver 8.8.8.8
# qm set 500 --ciuser ubuntu
# qm set 500 --cipassword SecurePass123!
# qm start 500
# Dan WordPress installeren en template maken


STAP 9: BASH SCRIPTS MAKEN
# Scripts zijn gemaakt in /mnt/pve/cephfs/cloud_computing_prac1/

# Test LXC script:
bash /mnt/pve/cephfs/cloud_computing_prac1/install-wordpress-lxc.sh 101 10.24.38.11

# Test VM script (vereist cloud-init enabled template VM 500):
# Template 500 heeft nu cloud-init, dus netwerk configuratie werkt automatisch!
bash /mnt/pve/cephfs/cloud_computing_prac1/install-wordpress-vm.sh 301 10.24.38.31 wordpress-client1

# Voor meerdere VMs - elk krijgt automatisch het juiste IP:
bash install-wordpress-vm.sh 302 10.24.38.32 wordpress-client2
bash install-wordpress-vm.sh 303 10.24.38.33 wordpress-client3
bash install-wordpress-vm.sh 304 10.24.38.34 wordpress-client4

STAP 10: ANSIBLE PLAYBOOKS
# Installeer Ansible op een control node
# Maak playbook voor LXC WordPress
# Maak playbook voor VM WordPress
# Maak playbook voor monitoring

STAP 11: HA CONFIGUREREN
# Via Proxmox GUI:
# Datacenter > HA > Add
# Voeg VM toe met strict policy
1. Via Proxmox GUI: **Datacenter > HA > Add**
2. Selecteer de WordPress VM
3. Kies **strict** policy (VM moet altijd draaien)
4. Test HA door een node uit te zetten (mail systeembeheer eerst!)

STAP 12: MONITORING
# Installeer monitoring tools (Prometheus/Grafana)
# Configureer alerts
Opties:
- Proxmox ingebouwde monitoring (basic)
- Prometheus + Grafana (geavanceerd)
- Zabbix
- Check_MK

=== GITHUB HTTPS AUTHENTICATION ===
# Op node1 (of elke node), setup git credentials
git config --global credential.helper store
git config --global user.name "Your Name"
git config --global user.email "your_email@example.com"

# Maak Personal Access Token (PAT) aan:
# 1. GitHub.com → Settings → Developer settings → Personal access tokens → Tokens (classic)
# 2. Generate new token (classic)
# 3. Selecteer scope: repo (full control)
# 4. Copy de token (ghp_xxxxxxxxxxxx...)

# Clone met HTTPS (eerste keer vraagt om credentials):
git clone https://github.com/Reinder15/cloud_computing_prac1.git
# Username: Reinder15
# Password: <paste PAT token>

# Token wordt opgeslagen in ~/.git-credentials voor toekomstig gebruik
